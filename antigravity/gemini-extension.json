{
  "name": "agent-aiko",
  "version": "0.1.0",
  "description": "Agent-Aiko persona layer for Antigravity and Gemini CLI.",
  "contextFileName": "GEMINI.md",
  "excludeTools": [
    "run_shell_command(rm -rf ~/.aiko)",
    "run_shell_command(rm -rf $HOME/.aiko)",
    "run_shell_command(sudo rm -rf ~/.aiko)",
    "run_shell_command(sudo rm -rf $HOME/.aiko)"
  ],
  "plan": {
    "directory": ".gemini/plans"
  }
}
