{
  "name": "@agent-aiko/cli",
  "version": "0.1.0",
  "description": "aiko コマンド — 状態確認と構成の点検。設計書 §4.4",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "aiko": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "pretest": "npm run build",
    "test": "node --import tsx --test test/*.test.ts",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@agent-aiko/adapter-claude-code": "*",
    "@agent-aiko/binder": "*",
    "@agent-aiko/core": "*",
    "@agent-aiko/runtime-sdk": "*",
    "@agent-aiko/user-context": "*"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/masa-san-jp/Agent-Aiko.git",
    "directory": "packages/cli"
  }
}
